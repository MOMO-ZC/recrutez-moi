import HeaderContainer from '../components/ui/HeaderContainer';
import { ScreenContainer } from './common/components';

const ProjectScreen = () => {
  return (
    <ScreenContainer>
      <HeaderContainer title="Project Screen" />
    </ScreenContainer>
  );
};

export default ProjectScreen;
